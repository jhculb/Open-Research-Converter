# Leave empty for the dockerised setup: the frontend then calls the backend on
# the same origin, routed through the nginx reverse proxy (/api). Only set this
# when running the frontend outside docker against a backend on another origin.
REACT_APP_DEV_URL=
REACT_APP_PROD_URL=https://orc-demo.gesis.org
REACT_APP_ENV=dev

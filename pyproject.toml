[build-system]
requires = ["poetry-core=1.5.1"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "Open Research Converter"
version = "0.0.1"
authors = ["Jack Culbert <jack.culbert@gesis.org>", "Ahsan Shahid <ahsan.shahid@gesis.org>", "Philipp Mayr <philipp.mayr@gesis.org>"]
maintainers = ["Jack Culbert <jack.culbert@gesis.org>", "Ahsan Shahid <ahsan.shahid@gesis.org>"]
description = ""
readme = "README.md"
packages = [{ include = "orc", from = "src" }]

[tool.poetry.dependencies]
python = "^3.11"
flask = "^3.0.3"
psycopg = "^3.1.18"
requests = "^2.31.0"
pandas = "^2.2.2"

[tool.poetry.group.dev]
optional = true

[tool.poetry.group.dev.dependencies]
bandit = "^1.7.5"
check-manifest = "^0.49"
pytest-cov = "^4.0.0"
pytest-mock = "<3.10.1"
pytest-runner = "*"
pytest = "^7.3.0"
shellcheck-py = "^0.9.0.5"
coverage = { extras = ["toml"], version = "^7.2.7" }
anybadge = "^1.14.0"
ruff = "^0.5.1"


[tool.ruff]
# Exclude a variety of commonly ignored directories.
exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".git-rewrite",
    ".hg",
    ".ipynb_checkpoints",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".pyenv",
    ".pytest_cache",
    ".pytype",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    ".vscode",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "site-packages",
    "venv",
]

# Same as Black.
line-length = 120
indent-width = 4

# Assume Python 3.12
target-version = "py312"

[tool.ruff.lint]
# Enable Pyflakes (`F`) and a subset of the pycodestyle (`E`)  codes by default.
select = ["E4", "E7", "E9", "F", "I"]
ignore = []

# Allow fix for all enabled rules (when `--fix`) is provided.
fixable = ["ALL"]
unfixable = []

# Allow unused variables when underscore-prefixed.
dummy-variable-rgx = "^(_+|(_+[a-zA-Z0-9_]*[a-zA-Z0-9]+?))$"

[tool.ruff.format]
# Like Black, use double quotes for strings.
quote-style = "double"

# Like Black, indent with spaces, rather than tabs.
indent-style = "space"

# Like Black, respect magic trailing commas.
skip-magic-trailing-comma = false

# Like Black, automatically detect the appropriate line ending.
line-ending = "auto"





[tool.bandit]
exclude_dirs = ["build", "dist", "tests", "scripts"]
number = 4
recursive = true
targets = "src"

[tool.isort]
profile = "black"


[tool.coverage.run]
branch = true
disable_warnings = ['no-data-collected']

[tool.coverage.report]
fail_under = 100

[tool.pyright]
include = ["src"]
exclude = ["**/node_modules", "**/__pycache__"]
venv = "env37"

reportMissingImports = true
reportMissingTypeStubs = false

pythonVersion = "3.11"
pythonPlatform = "Linux"

executionEnvironments = [{ root = "src" }]

[tool.pytest.ini_options]
addopts = "--cov-report xml:coverage.xml --cov-report term --cov src --cov-fail-under 0 --cov-append -m 'not integration'"
pythonpath = ["src"]
testpaths = "tests"
junit_family = "xunit2"
markers = [
    "integration: marks as integration test",
    "notebooks: marks as notebook test",
    "gpu: marks as gpu test",
    "spark: marks tests which need Spark",
    "slow: marks tests as slow",
    "unit: fast offline tests",
]

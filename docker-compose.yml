version: '3.3'

services:
  nginx:
    container_name: reverse-proxy
    hostname: reverse
    image: nginx:latest
    depends_on:
      - frontend
      - backend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - nginx_data:/etc/nginx
      - nginx_data:/etc/ssl/private
    restart: always
    networks:
      - frontend

  frontend:
    container_name: frontend
    build:
      context: ./src/orc/frontend/
    ports:
      - "80:80"
      - "443:443"
    networks:
      - frontend
      - backend

  backend:
    container_name: orc-backend
    build:
      context: ./src/orc/backend/
    ports:
      - "80:80"
      - "443:443"
    restart: always
    networks:
      - backend

volumes:
    nginx_data: {}

networks:
  frontend:
  backend: